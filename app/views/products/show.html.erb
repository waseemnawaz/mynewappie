<p id="notice"><%= notice %></p>

<div class="container anchor introshow">
	<%#= image_tag @product.image_url, class: "img-responsive bigshowpic" %>
	<h3>
  	<%= @product.name %>
	</h3>
	<h4>
  	<%= @product.description %>
	</h4>
	<%= image_tag(@product.image_url, class: "img-zoom img-responsive bigshowpic", data_zoom_image: @product.image_url) %>
</div>

<div class="container bigpicc">
	<% cache @product do %>
	<div class="row">
		<!--div class="col-md-3">
			<h4>
  			<span class="glyphicon glyphicon-home" aria-hidden="true"><strong> Name:</strong></span>
  			<%#= @product.name %>
			</h4>
		</div-->
		<div class="col-md-4">
			<h4>
  			<span class="glyphicon glyphicon-usd" aria-hidden="true"><strong> Price:</strong></span>
  			<%= @product.price.to_d/100 %>
			</h4>
			<%= form_tag "/payments/create" do %><%= hidden_field_tag(:product_id, @product.id) %>
  				<%= render partial: "shared/stripe_checkout_button" %>
  			<% end %>
		</div>
		<!--div class="col-md-3">
			<h4>
  			<span class="glyphicon glyphicon-zoom-in" aria-hidden="true"><strong> Description:</strong></span>
  			<%#= @product.description %>
			</h4>
		</div-->
		<div class="col-md-4">
			<h4>
  			<span class="glyphicon glyphicon-pencil" aria-hidden="true"><strong> Colour:</strong></span>
  			<%= @product.colour %>
  		</h4>
  	</div>
  	<div class="col-md-4">
  		<p>Average Rating: <div class="rated" data-score="<%= @product.average_rating %>"></div>
  			<%#= form_tag "/payments/create" do %><%#= hidden_field_tag(:product_id, @product.id) %>
  				<%#= render partial: "shared/stripe_checkout_button" %>
  			<%# end %>
  		</p>
		</div>
	</div><!-- row -->
	<% end %><!-- cache -->
</div><!-- container -->

<%#= render 'comments' %>

<%= render 'new_comment' %>
<%= render 'all_comments' %>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>

